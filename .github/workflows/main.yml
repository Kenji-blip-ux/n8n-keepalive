name: Keep n8n Alive

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n Render
        run: curl -s https://n8n-kenji.onrender.com > /dev/null



